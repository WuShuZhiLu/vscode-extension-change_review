## 0.5.1
- 修复强制中文时原生提示仍显示英文的中英混用
- 运行时提示、面板、来源文案统一跟随 `changeReview.uiLanguage`（zh / en / auto）
- 新增语言模式测试：zh / en 下提示与面板 UI 均不混用

## 0.5.0
- 修复extension.js未支持多语言
- 完善README文档，changelog内容去重，同步中英文档内容
- 重绘扩展图标 
- 移除无用的 preview 生成页

## 0.5.0-pre1
- 接受 / 拒绝（文件级与块级）只记录决定，不改文件；git add / 还原 / 删除在「标记已审查」那一刻统一执行
- 所有块都有决定后自动标记已审查，并跳到下一个待审查文件
- 新增「取消接受」，与「撤销拒绝」对称
- 手动「取消审查」后文件未再改动不会被自动打回，改动后恢复自动标记
- 多仓库工作区：打开目录不是仓库时自动扫一层子目录
- 「拒绝全部」不再弹确认框
- 树右键菜单分工：文件节点「忽略该文件」（写 `.crignore`），仓库节点「配置排除规则…」；「清除所有审查标记」更名「全部清除已审查」
- 修复 diff 正文右键菜单弹不出、审查中标记跨刷新丢失
- 面板按钮：已审查显示「取消审查」，未审查显示「标记已审查」
- 排除规则无 `.crignore` 时回退 `.gitignore`；「忽略该文件」只保留在面板右键菜单
- 工程：`mockcheck --only <场景>` 按场景跑；release 用预装 vsce；新增 CI/CD，打 tag 自动发版

## 0.4.20
- 修复块全部打钩却没标记已审查

## 0.4.19
- 修复「忽略该文件」写入 `.crignore` 后不生效，排除匹配对齐 `.gitignore` 语义

## 0.4.17
- 新增「忽略该文件」：一键把当前文件写入所在目录 `.crignore`，立即生效并从列表消失

## 0.4.15
- 整行选中时 Backspace / Delete 不再误合并行
- 撤销 / 重做覆盖行间操作：快照栈移到扩展侧，跨面板重渲染不丢

## 0.4.14
- Ctrl+C / V / X 由面板接管剪贴板，不再依赖 `execCommand`
- 行级编辑只复查当前文件，不再整仓刷新
- 合并行与删行：Backspace 在行首并进上一行，Delete 在行尾并上下一行，Ctrl+Shift+K 整行删除

## 0.4.13
- Tab 缩进跟随 `editor.insertSpaces` / `editor.tabSize`
- Enter 插行按光标位置：行首上方插、行尾下方插、行中拆行
- Ctrl+S 保存当前行；Ctrl+Z / Ctrl+Shift+Z 撤销重做

## 0.4.12
- 移除树视图标题栏的「下一个待审查」图标按钮（面板内文字按钮保留）

## 0.4.11
- 面板移除「下一个待审查」按钮，树命令与审查完成后的自动跳转保留
- 修复 Linux「0 改动」假象：目录与仓库根路径不一致导致 pathspec 匹配不到
- 扩展图标白色背景改真透明
- `changeReview.uiLanguage` 设为 `zh` 可强制中文（auto 跟随 VSCode 显示语言）

## 0.4.10
- 修复「部分拒绝部分接受」后已审查标记丢失（内容指纹失配）
- 日志落盘到 `globalStorage/change-review.log`，命令面板「打开日志」可查看

## 0.4.9
- 修复拒绝块无视觉变化：拒绝状态从未渲染

## 0.4.8
- 内容与 0.4.7 二轮相同，独立升号以便区分安装包

## 0.4.7
- 拒绝块改为延迟执行，与接受块对称：只记录决定，显示红色「拒绝·待执行」徽章
- 修复拒绝块仍立即还原（自动标记把拒绝也算成完成）
- 新增设置 `changeReview.uiLanguage`（auto / zh / en）
- 新增命令「打开日志」；错误弹窗自动打开输出面板
- 「下一个待审查」去掉防重入锁（异常路径曾卡死）
- 文件整体标记已审查后隐藏块级接受 / 拒绝按钮

## 0.4.6（本地打包实测用 · 未发布到市场；版本号冻结，后续修改不再升号，直到用户同意）
- 面板文字中英双语，随 VSCode 显示语言切换
- 换用正式插件图标（深底白文件 + 绿对勾），新增市场图标 `resources/icon.png`
- 修复面板所有按钮与快捷键失效
- 复制改由扩展宿主写系统剪贴板
- 修复「拒绝全部」不删除新增文件（进过暂存区后 kind 变 added）
- 修复 SVN 拒绝新增文件后留下空文件
- 「跳转」改为打开 VSCode 原生左右对比视图并定位到改动行
- 多行选择：点行号选中、Ctrl/Cmd 与 Shift 多选、双击选整段、Ctrl+A 全选；浮动工具条支持复制 / 删除 / 还原此段
- 块内支持「段」级还原
- 树高亮显示 `◀ 审查中` 并跟随切换
- Linux git 诊断：解析出 0 条改动时输出原始结果；兼容引号路径

## 0.4.5
- 接受 / 拒绝不再碰 git 暂存区，只标记决定并给文件打钩
- 移除行级「+行 / 删行」按钮，增删行只走键盘
- 编辑时 ↑/↓ 在可编辑行间切换，失焦自动写回

## 0.4.4
- 修复删行后焦点跳错行

## 0.4.3
- 修复「下一个待审查」后改动列表高亮不跟随（`treeView.reveal` 滚动并选中）
- 修复无管理项目「更新基准」只更新当前选中文件
- 相近改动恢复合并在同一块（撤销 0.4.2 的按簇拆分），与 `git -U3` 一致
- 对比块支持键盘删行：空行 Backspace / Delete，任意行 Ctrl/Cmd+Delete

## 0.4.2
- 修复 `.crignore` 目录排除无效（`build/` 这类写法）
- 统一 diff 增删行配色，与按钮同色系
- 对比块支持增删行：Enter 在下方插行并聚焦，Esc 放弃修改
- 修复无管理项目「更新基准」后差异仍在（untracked 文件进不了基准）

## 0.4.1
- 修复块间移动时块忽大忽小（按钮改用 visibility 隐藏）
- 统一按钮风格：共用描边基础，只用颜色区分语义
- 删除残留的「显示编辑器」按钮

## 0.4.0
- 「编辑」= 在对比块里就地改，移除单独「编辑」按钮，每块只留「跳转」
- 修复「跳转」慢、先停首行再跳
- 排除文件改名 `.changereviewignore` → `.crignore`
- 不做 `.crignore` 文件图标：VSCode 无单文件图标 API，需整套图标主题，代价大于收益

## 0.3.9
- 移除内嵌编辑器（隐藏 textarea「自由编辑」）

## 0.3.8
- 移除「诊断环境」命令，诊断信息本就写入 Output 面板
- 「配置排除规则」改为直接编辑项目里的排除规则文件，不再跳插件设置
- 接受 / 拒绝语义移入悬浮提示
- 对比块改为卡片式（圆角边框 + 主题色增删行背景 + 左侧色条）

## 0.3.7
- 修复标题栏图标仍是文字（`view/title` 需显式声明 icon）
- 新增内嵌编辑器「自由编辑」
- 块级「跳转」改为面板内跳

## 0.3.6
- 接受 / 拒绝括号里的语义改为 tooltip
- 去掉「在编辑器中打开并修改」按钮
- git / svn / 快照的判定结果直接可见
- 多语言支持：`package.nls.json` + 中英双语
- Activity Bar / 标题栏图标换新
- 新增行配色更鲜艳

## 0.3.5
- 标题栏精简为「刷新」「下一个待审查」两个图标按钮
- 去掉命令标题的 "Change Review: " 前缀
- 跳转 / 编辑改为打开真实文件
- 新增命令「配置排除规则…」，入口放在标题栏「…」、右键菜单、欢迎页
- 性能优化

## 0.3.4
- 标题栏新增图标按钮：初始化基准、更新基准、全部标记已审查、全部清除
- 新增键盘快捷键，仅在视图聚焦时生效

## 0.3.3
- 排除规则改名通用 `changeReview.exclude`（取代 `svnIgnore`）
- 标题栏「刷新」「下一个待审查」改图标按钮

## 0.3.2
- SVN 未版本化项支持忽略规则
- 向上探测上限锁死 5 层
- 内置快照排除与 SVN 忽略统一为一份

## 0.3.1
- 修复打开仓库子目录时列出整个仓库的改动
- 诊断输出增加「打开目录范围」

## 0.3.0
- 支持 SVN 与无版本控制项目（快照基准），来源自动探测 git → svn → 快照，可用 `forceVcs` 强制
- SVN：`.svn` 按 `vcsSearchDepth`（默认 5 层）逐级向上探测
- 快照基准存 globalStorage，不污染项目；可初始化 / 更新（支持只更新选中文件）
- 新命令：「初始化对比基准」「更新对比基准」
- 新配置：`svnPath`、`forceVcs`、`vcsSearchDepth`、`snapshotExclude`、`snapshotMaxFileSize`、`snapshotMaxFiles`
- 块级拒绝改为纯 JS 反向替换，三种来源行为一致
- 内置纯 JS 行级 diff 引擎（前后缀裁剪 + Myers + 公共锚点），超大差异自动退化为整段替换
- 诊断命令扩充：来源类型 / 基准 / 文件数 / 快照位置

## 0.2.0
- 修复审查面板按钮全部无效
- 新增按块接受 / 拒绝
- 面板消息与 git 动作写入 Output 面板
- 块与当前 diff 对不上时提示刷新，不再误操作其它块

## 0.1.1
- 平台适配：Windows / Linux(WSL) / macOS 各自 git 候选路径，自动探测并缓存；识别路径体系不匹配并切换候选
- `extensionKind: workspace`，确保 WSL / 远程以 workspace 运行；远程工作区跑在本地宿主时告警
- git `dubious ownership` 提示一键加 `safe.directory`
- 命令取不到目标文件时回退：树选中项 → 当前编辑器文件 → 面板当前文件
- 命令统一 try/catch，失败弹通知 + 写 Output
- 已审查文件右键菜单也显示审查相关命令
- 新增命令「诊断环境」与设置 `changeReview.gitPath`
- 修复命令包装器吞返回值、「全部标记已审查」未同步内存状态导致徽章不更新

## 0.1.0
- 列出与上次 commit 不同的本地文件，显示 `+新增 / −删除` 行数
- 每个文件可打钩标记已审查，状态持久化；文件再改动自动取消打钩
- Activity Bar 徽章 + 状态栏进度
- 点击文件名进入审查模式：统一视图 diff、按块还原、接受 / 拒绝
- 原生 diff 编辑器打开，右侧可直接编辑
- 「下一个待审查」循环跳转，多仓库工作区分组
