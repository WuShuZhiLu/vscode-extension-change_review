# Change Log

## 0.4.20

- **面板按钮文案**：文件已审查后，按钮直接显示「取消审查」（不再用「已审查 ✓（点击取消）」的括号提示）；未审查时显示「标记已审查」。文案同时纳入中英 i18n（之前是硬编码中文，英文界面下也是中文）。
- **修「块都打钩了，却没标记已审查」**：
  - **崩溃点修掉**：`parseDiff(t)[0].hunks` 在 diff 文本为空时（典型：审查途中文件被外部还原 / 被外部接管）会抛 `TypeError`，被外层 catch 吞掉后只留一行日志 —— 用户看到的就是"点了没反应、也没标记"。现在全部改成先判空，并给出明确提示「文件内容已变化，该块位置对不上了，请刷新后重试」。
  - **加兜底对账**：块级检查原先只在「点击接受/拒绝的那一刻」跑一次，任何原因漏掉（模型对象被自动刷新换掉、面板停在旧 entry、并发点击）就再也不会补。现在每次刷新收尾统一对账一遍：只要某个文件「当前所有改动块都有决定（接受/拒绝）」但还没打钩，就补上标记。这条兜底保证「全打钩」= 已审查。
  - **指纹口径统一**：标记时写进 store 的 hash 改为优先取 provider 复查出的权威值，和刷新时算 hash 的地方完全同源，避免「刚标记就被下一次刷新判成未审查」。
  - 不满足自动标记时会把原因写进日志（`Change Review: 打开日志`）：还有几个块没决定 / 解析不到改动块 / 对账补标记，都能查。
- 测试：mockcheck 新增 [23]，覆盖 4 种真实顺序 —— 全接受、拒绝一块+接受一块（含还原后再全量刷新标记不掉）、接受一块后中途全量刷新再补其余、审查途中文件被外部还原（必须给提示而不是静默失败）。已做反向验证：把 parseDiff 判空改回旧写法，[23] D 立刻失败。

## 0.4.19

- **修复「屏蔽此文件」写入 `.crignore` 后不生效**，并把排除匹配**对齐 `.gitignore` 语义**。
  - **根因**：`.gitignore` 的规则匹配的是「相对**该 .gitignore 所在目录**」的路径；我们之前把 `.crignore` 的规则拍平成一个列表，统一按「相对**来源根**（git 仓库根 / svn 工作副本根 / 快照根）」去匹配。当来源根 ≠ 规则文件所在目录时（典型：VSCode 打开的是仓库的子目录），前缀对不上，规则永远匹配不到 → 屏蔽无效。
  - **修法**：项目忽略文件（`.crignore` / 回退用的 `.gitignore`）的规则改为**带上它所在的目录**交给各来源，匹配时按「相对该目录」的路径比对（`util.matchExcludeSets`）——和 git 完全一致。`changeReview.exclude` 设置项仍按「相对来源根」匹配。
  - 「屏蔽此文件」写入的规则回到「相对该 `.crignore` 所在目录」，也就是 `.gitignore` 的自然写法；**已经写好的规则不需要手动改**。
  - 另外修掉两个会让它看起来"没生效"的坑：右键屏蔽前先等正在进行的刷新结束再强制重建（避免刷新锁）；写入后若文件仍在列表，把「规则 / 基准」写进日志（`Change Review: 打开日志`）。
  - 说明：0.4.18 曾把规则改成「按来源根写」并加「任意层级」兜底，方向不对（那是迁就旧的匹配口径），已回退。
- 测试：selfcheck 新增 [8.1]（`matchExcludeSets` / `relFromBase` 锚定语义，含"不误伤规则文件目录之外的同名路径"）；mockcheck [22]（打开仓库子目录时规则仍命中）；vcscheck 新增真实 SVN 仓库用例（svn 根在外、只打开子目录，`.crignore` 规则命中且不误伤同级文件）。三套全绿，并已验证"回退修复 → 测试失败"。

## 0.4.17

- **新增「屏蔽此文件」**：审查面板里可一键把当前文件写入所在工作区目录的 `.crignore`（gitignore 风格相对路径规则），写完后立即重新探测刷新，文件从改动列表消失。
  - 面板**右键菜单**（新增）：在面板任意位置右键弹出，含「打开新旧对比 / 接受全部 / 拒绝全部 / 标记(取消)已审查 / 屏蔽此文件 / 复制文件路径 / 刷新列表」。行内编辑区右键仍保留系统「复制/粘贴」菜单。
  - 面板**工具栏**也加了「屏蔽此文件」按钮。
  - 树视图文件项右键菜单与命令面板同样提供「屏蔽此文件」，文案随 VSCode 语言中英切换。
  - 规则幂等：已在 `.crignore` 中的路径不会重复写入；屏蔽后若当前文件已从列表消失，自动跳到下一个待审查。
- mockcheck 新增 [9.3.1]：右键屏蔽写入 `.crignore`、文件从列表消失、重复屏蔽不重复追加；面板 HTML 含右键菜单容器与按钮。

## 0.4.15

- **排除规则回退到 .gitignore**：没有 `.crignore` 时，默认采用该目录 `.gitignore` 的规则（git 工程不用再额外配 `.crignore`）；有 `.crignore` 时仍以它为准。
- **整行选中 + Backspace 不再错误合并**：合并行只在「无选区且光标贴边」时触发；有选区时按 Backspace/Delete 走正常删除内容。
- **撤销/重做覆盖行间操作**：新增文件级快照栈（扩展侧，跨面板重渲染不丢）。行内文字仍用细粒度栈；行内栈空时 Ctrl+Z / Ctrl+Y 自动走文件级撤销重做，**新增行、删除行、合并行**都能撤销恢复。
- **修复"撤销→重做→再撤销失效"**：根因是面板重渲染会把 webview 里的撤销栈清空，现在栈放在扩展侧，不再受重渲染影响。
- mockcheck 新增：gitignore 回退、合并行需无选区、文件级撤销/重做（插行可撤销、可重做、再撤销仍有效）。

## 0.4.14

- **Ctrl+V / Ctrl+C / Ctrl+X 真正生效**：不再依赖浏览器默认行为和 `execCommand`（webview 里常被 VSCode 拦下），改为面板主动调 `navigator.clipboard.readText/writeText` 读写剪贴板并插入/删除选区；Ctrl+Z / Ctrl+Y（Ctrl+Shift+Z）同样 preventDefault + stopPropagation，撤销栈空时给日志提示。
- **行中回车不再卡**：行级编辑（写回、插行、删行、合并）的收尾从"整仓全量刷新 doRefresh"改成"只复查这一个文件 refreshSingleEntry"，去掉整仓 git 扫描，行内编辑响应明显变快。
- **合并行与删行**：Backspace 在行首 → 并进上一行；Delete 在行尾 → 把下一行并上来；Ctrl+Shift+K（编辑器同款）可整行删除内容行，空行 Backspace/Delete、Ctrl+Del 仍旧删行。
- mockcheck 新增断言：上行/下行合并（文件内容与行数实测）、剪贴板自行接管、行内编辑不再触发全量刷新。

## 0.4.13

- **Tab 跟随 VSCode 设置**：面板内联编辑的 Tab 插入内容按 `editor.insertSpaces` / `editor.tabSize` 决定（空格还是制表符、几个空格），不再固定插 `\t`。
- **Enter 插行方向按光标位置**：光标在行首 → 在本行**上方**插空行（焦点留在原内容行，内容顶下去）；光标在行尾 → 下方插空行；光标在行中 → 像编辑器一样**拆行**（前半留本行、后半成下一行）。新增 `insertAbove` 消息与 `insertLineAboveInFile`。
- **Ctrl 组合键在行编辑中生效**：Ctrl+S 保存当前行（内容已最新时给明确提示，不再静默）；Ctrl+Z / Ctrl+Shift+Z(Ctrl+Y) 撤销重做（自维护撤销栈，跨面板重渲染仍有效）；Ctrl+C / Ctrl+X 走原生剪贴板；Ctrl+A 只选中当前行；Ctrl+V 由 paste 统一处理。
- mockcheck 新增 [18.1]：上行插行、拆行、Tab 缩进注入、快捷键接管均有实测断言。

## 0.4.12

- 纠正删除对象：移除的是树视图标题栏的「下一个待审查」**图标按钮**，panel 里的文字按钮保留。

## 0.4.11

- **面板移除「下一个待审查」按钮**（反复出问题，弃用）；树视图命令与审查完成后的自动跳转保留。
- **修复 Linux"0 改动"假象**：打开目录与仓库根路径不一致（符号链接/挂载点）时，`../..` 被当作 git pathspec 传入导致匹配不到任何文件。现发现 scope 不在 root 之下时退化为整仓库扫描；git status 空结果时记录 root/pathspec 到日志（每组合一次）。
- **扩展图标白色背景改真透明**：圆角方块外的四角像素 alpha 置 0。
- 面板语言说明：`changeReview.uiLanguage` 设为 `zh` 可强制中文（auto 跟随 VSCode 显示语言；VSCode 本体是英文界面时 auto 会判成英文）。

## 0.4.10

- **修复"部分拒绝部分接受"后已审查标记丢失**：拒绝块执行还原后文件内容变化，旧的按内容指纹校验立刻失配，文件显示回"未审查"。现在标记时若执行过拒绝还原，会按还原后的实际内容重算指纹再记录，标记稳定不丢。
- **Linux 诊断强化**：日志同时落盘到 `globalStorage/change-review.log`（终端可直接 cat/tail，Output 面板找不到时日志也不丢）；git 层所有命令失败、status 返回异常等诊断统一进日志；每次扫描记录各源文件数与错误。
- 日志查看：命令面板「Change Review: 打开日志」，或终端 `cat ~/.../globalStorage/local/change-review.log`。

## 0.4.9

- **全部块都有决定（接受或拒绝）→ 自动标记已审查**：拒绝块在此刻统一执行还原，随后立即复查该文件。
- **文件审查完成后自动跳下一个**：拒绝执行还原后文件无差异 → 从列表移除并自动跳到下一个待审查；手动「标记已审查」同理。全部审查完时面板换成"所有文件都已审查完毕 ✓"，不再停留在过期内容或"该文件没有差异"空面板。
- **修复拒绝块无任何视觉变化**：根因是 `rejectedSigs` 没传进块渲染函数，拒绝状态从未被渲染。现在拒绝块显示：头部红底 + 整块行变暗 + 红色左条 + 「拒绝·待执行」徽章 + 「撤销拒绝」按钮。

## 0.4.8

- 与 0.4.7 二轮内容相同（拒绝块不再立即删除、uiLanguage 语言设置、日志可见性、next 去锁），独立升号以便区分安装包。规则确立：**每轮修复自动升号，不再冻结。**

## 0.4.7

- **修复拒绝块仍立即还原内容**：根因是「全部块都有决定就自动标记已审查」把拒绝决定也当成了完成——单块文件点拒绝会瞬间触发自动标记并执行还原，看起来就是"直接删除"。现改回：只有全部接受才自动打钩；含拒绝决定必须手动「标记已审查」才执行还原，反悔机会保住。
- **面板语言可强制指定**：新设置 `changeReview.uiLanguage`（auto / zh / en），auto 跟随 VSCode 显示语言。面板右下角版本行会显示实际检测到的语言（lang=xx · ui=xx），若显示不对一眼可查。
- **日志终于能看到了**：新增命令「Change Review: 打开日志」；所有错误弹窗自动打开输出面板；日志同步写 console（Developer Tools 可见）；扩展激活即写一条日志（含版本与语言检测）。
- **「下一个待审查」去掉防重入锁**（锁曾在异常路径卡死导致只有第一次生效），每一步写日志、出错弹窗并自动打开日志面板。
- 拒绝块延迟执行（与接受块对称）、拒绝全部后自动标记已审查（0.4.7 首轮引入，继续生效）。

- **拒绝块改为延迟执行（与接受块对称）**：点「拒绝此块」只记录拒绝决定，不再立即改文件；块上显示红色「拒绝·待执行」徽章与红色左条，按钮变「撤销拒绝」——标记已审查之前随时可反悔。点「标记已审查」时统一执行所有待执行的拒绝块（从后往前还原，避免行号漂移），执行失败则不标记，保持状态一致。接受块同时会自动清掉该块的拒绝记录，反之亦然。
- **拒绝全部后自动标记已审查**：与接受全部对称，还原完成后文件自动打钩并从改动列表消失。
- 文件整体标记已审查后，块级「接受/拒绝」按钮隐藏（0.4.6 已引入，本版继续生效），跳转保留。

## 0.4.6（本地打包实测用 · 未发布到市场；版本号冻结，后续修改不再升号，直到用户同意）

- **面板文字 i18n（中英双语）**：面板内所有文字（按钮、提示、tooltip、状态标签）随 VSCode 显示语言自动切换。中文 VSCode 显示中文，英文 VSCode 显示英文。实现：`PANEL_I18N` 字典 + `vscode.env.language` 检测。
- **插件图标（AI 生成）**：用 ImageGen 生成正式图标（256x256 PNG，深底白文件+绿对勾），替换掉之前手绘的丑图标。Activity Bar SVG 也重新绘制（内容撑满画布，减少空白）。
- **Linux git 诊断**：`git status` 返回数据但解析出 0 条改动时，把原始输出写到 stderr 帮助定位 Linux 解析问题；`parseDubiousRoot` 兼容 Linux 的引号路径。

- **Activity Bar 图标重绘**：内容撑满画布（边距从 22px 缩到 4px），减少空白；插件包新增 `resources/icon.png`（扩展列表/市场图标，package.json `icon` 字段）。
- **「下一个待审查」加防重入锁 + 错误上报**：连续快速点击不再互相干扰；如果内部出错，用户会看到错误提示而不是静默无反应。
- **接受全部后隐藏块级按钮**：文件整体已接受（已审查）后，块级「接受此块 / 拒绝此块」按钮自动隐藏，避免矛盾操作。

- **树高亮"审查中"标记（不依赖 reveal）**：当前正在审查的文件在改动列表的 description 里显示 `◀ 审查中` 后缀——这是我们自己渲染的，不依赖 `treeView.reveal` 的视觉高亮是否生效。配合 reveal（滚动定位）双保险：reveal 负责把视图滚到那行，`◀ 审查中` 负责让肉眼一眼看到。每次点文件名 / 下一个待审查 都会刷新标记。

- **交互方向纠正（用户明确要求）**：「跳转」不再打开单文件编辑器，改为**打开 VSCode 原生左右对比视图**（左=基准只读，右=工作区文件），并且光标直接定位到改动行。右侧就是正常文本编辑器——多行选、复制、粘贴、删除全部原生支持，不需要我们在 webview 里造轮子。整套 webview 多选/簇还原/多行粘贴代码全部移除（selbar、data-h/clus、insertLinesBelow、clusterRestore 等），webview 面板回归"概览 + 接受/拒绝 + 跳转"的定位。

- **修复「拒绝全部」对新增文件不删除（真根因）**：新增文件只要进过暂存区（标记已审查 = git add），git 状态从 `??` 变成 `A`（kind=added），旧代码走"还原到 HEAD"分支——该文件从未提交，restore 必然报错。现在 `untracked` 与 `added` 一律按删除新文件处理（`git rm -f` 同时移出暂存区+删盘，未跟踪则直接删），已用真实 git 仓库验证两种状态拒绝后文件消失、status 清空。
- **修复复制不可靠**：复制改由扩展宿主写系统剪贴板（webview 内 execCommand('copy') 不稳定）；另外补了兜底：没点进某行但有选中行时，Ctrl+V 直接插到最后选中的文件行下方。
- **树高亮跟随再加两道保险**：把"当前该高亮的文件"记下来；改动列表从隐藏变可见时、以及每次刷新完成后，都会自动再补一次 reveal——解决列表不在前台期间 reveal 静默失败、回到列表时高亮对不上的问题。
- **修复面板"什么功能都失效"的真正兜底**：用 jsdom 实测确认脚本在正确环境下能跑（点行号→选中、双击→整段、复制→`copyText` 全部正常），但用户实测看不到效果——根因是 VSCode 升级扩展后旧 webview 仍保留老 HTML 残影。已做三件事：①`retainContextWhenHidden: false`（每次打开都用新脚本）；②整个内联脚本包在 `try/catch` 里，单点异常 post 回扩展输出面板；③面板右下角加版本徽标 `Change Review v0.4.6`——若你看到的不是这个版本，就是旧 webview 在捣鬼，关掉面板重开即可。
- **修复 SVN「拒绝」新增文件留空文件的 bug**：原来 `svn revert` 在"文件已被 `svn add` 但未提交"或"BASE 是空文件"两种场景下会把工作区文件写空，但用户期望"全清零"应直接删。现已：(a) `kind=added` 走 `unlink + svn revert`；(b) `kind=modified` 但 `svn revert` 后文件变 0 字节时，自动再 unlink 掉这个空文件。

- **修复面板所有按钮/快捷键失效**（拒绝、接受、标记已审查、Tab、Ctrl+S、多选、换行全部没反应，0.4.6 首轮实测发现）。根因：面板 HTML 由模板字符串生成，新增代码里的 `'\n'` / `'\t'` / `/\s/` 被模板字面量先行转义成真实换行/制表符，嵌进内联脚本后把字符串字面量打断 → 整段脚本语法错误 → 事件监听器根本没挂上。已改双重转义，并新增回归断言：mockcheck 用 `new Function` 对生成的内联脚本做纯解析校验，防止复发。
- **行编辑 Tab = 制表符**（不再跳焦点）；**Ctrl+S = 立即保存该行并刷新 diff**，保存后光标仍留在该行。
- **树高亮跟随再次加固**：点「下一个待审查」改为"先试高亮 → 切面板 → 再补等一轮"两段式；面板里「标记已审查/接受」后也会 reveal，打钩文件下移时高亮跟着走。
- **多行选择 + 复制/删除/粘贴（按用户确认的方案）**：点行号选中、Ctrl/Cmd+点多选、Shift 连选、双击行号选中整段、Ctrl+A 全选；浮动工具条「复制 / 删除选中 / 还原此段 / ✕」，Ctrl+C 复制（删除行复制被删原文）、Esc 取消。多行粘贴（Ctrl+V 含换行）= 插到当前行下方并逐行写入文件；「删除选中」批量删除当前文件中存在的行。
- **块内"段"级还原**：一块里的每一段连续改动（如"上面一处修改 + 两行后一处纯删除"）可单独「还原此段」——只把这段改回对比基准（新增行移除 / 删除行加回），不影响其它段。实现：`clustersOf` 切段 + 合成迷你 hunk 走既有 `revertHunkInText`，git / svn / 快照三来源都支持；selfcheck 新增 `[10]` 端到端（两段合一块、只还原删除段、修改段保留）。

## 0.4.5

- **接受/拒绝不再碰 git 暂存区（语义重构）**：接受（全部/此块）只把改动标记为已接受并给文件打钩——不改内容、不 `git add`；git 与 svn/快照基准行为统一。拒绝（全部/此块）仍是对文件内容的还原。**git add 只在显式「标记已审查」打勾时发生**（勾选 = `git add`；取消 = `git reset` 撤出暂存、工作区不动，且只有文件确在暂存区才撤，避免误撤手动暂存）。删除了废弃配置 `changeReview.acceptStages`，面板悬浮文案同步更新。
- **移除行级「+行 / 删行」按钮**（用户要求不要了）：增删行只走键盘——Enter 下方插行、空行 Backspace/Del 删行、Ctrl+Del 删整行。
- **编辑行时 ↑/↓ 在可编辑行之间切换**：失焦自动写回当前行，光标落到上一/下一行。
- **修复「下一个待审查」后树高亮与面板不一致**：找不到节点时中途强制刷新树再重试 reveal（`expand` 展开父节点），避免列表高亮停在旧文件。

## 0.4.4

- **修复删行后焦点跳错行**（如：删 19 行焦点跑到 21 行、20 行原地不动；20 行回车出的空行删掉后不回 20）。根因：删完重渲染后按「删掉的行号」硬指焦点，diff 重算后行号会漂移。
  现在删行前先在面板侧判定被删行内容：**空行**（通常是刚回车出来的）→ 删完焦点回到**上一行**（正是你期望的"回车到 21 删掉后回 20"）；**内容行**（删行按钮 / Ctrl+Del）→ 焦点留在原位、由下一行顶上来继续往下审。不再依赖删完重算的行号。

## 0.4.3

- **修复「下一个待审查」后面板切了、改动列表高亮不跟随**：树数据源现在会记录每个文件的当前节点实例，切下一个文件后用 `treeView.reveal` 把对应节点滚动到可见并选中，面板与改动列表始终指同一个文件。
- **修复无管理项目「更新基准」只更新了当前选中的那一个文件**（0.4.2 bug 10 截图现象：package.json 清了、其余文件差异原样）：`updateBaseline` 之前走 `resolveArg` 的兜底链（树选中 → 面板当前文件），点标题栏 ↑ 意图是「全量更新」，却静默回落成单文件更新。现在只有**显式传了文件级参数**（单文件入口）才更新单文件；标题栏 ↑ / 命令面板 / 仓库右键一律全量更新。已加回归：树里选中某文件时点 ↑，所有文件差异仍全部清零。
- **相近的多处改动恢复合并在同一块**（撤销 0.4.2 的按簇拆分）：行内相近修改就是同一处编辑意图，保持与 `git -U3` 输出一致，一个块整体接受/拒绝；`splitHunk` 相关代码全部移除，`selfcheck [9]` 改为断言「间隔 1 行的两处改动仍是 1 个块」。
- **对比块里支持键盘删行**：空行上按 **Backspace / Delete** 删除该行（与 Enter 插入行对称）；任意行按 **Ctrl/Cmd+Delete** 直接删整行。删除后焦点落到补位行。

## 0.4.2

- **修复 `.crignore` 文件夹排除无效**：glob 匹配只认 `**/dir/**` 形式，`build/`、`.vscode/`、`test/` 这类 gitignore 目录写法生成的正则是 `^build/$`，什么都匹配不上。现在按 gitignore 语义完整支持：`build/`（任意层级目录及内容）、`/build`（根锚定）、`build`（任意层级同名文件/目录）、`*.log`、`.tmp.*`、`src/x.js`、`**/x/**` 均按预期工作（15 个用例固化进 `selfcheck [8]`）。
- **统一 diff 增删行颜色**：新增行背景不再用主题变量 `diffEditor-insertedLineBackground`（那是另一种绿，和按钮的 `#3fb950` 对不上），改为直接由按钮/徽章同款色相派生（新增 `rgba(63,185,80,.20)`、删除 `rgba(248,81,73,.16)`），全面板只有一套绿和红。
- **对比块编辑支持增删行**：每行悬浮出现「+行 / 删行」按钮（绝对定位 + opacity，不抖动布局）；编辑时按 **Enter** 直接在该行下面插入一行并自动聚焦新行（Esc 仍是放弃修改）。`insertLine`/`deleteLine` 按真实行号写回文件并自动刷新。
- **修复两处相近改动被并进一个块**：`git -U3` / 内置 diff 引擎会把间隔不超过上下文行数的几处改动合并成一个 hunk，导致「接受/拒绝此块」连带无关改动。现在 `parseDiff` 在解析时按改动簇自动拆分（簇间上下文归后一块作前导），每处改动独立成块、独立接受/拒绝；行号按内容重算，拒绝操作的端到端路径已验证（拆出的每块都能精确还原）。
- **修复无管理项目「更新基准」后差异仍在**：全量更新基准只遍历已有基准索引，**untracked 新文件永远进不了基准**。现在全量更新会把当前扫描到的所有文件（含新文件）一并纳入基准。
- 测试：`selfcheck` 新增 `[8]` 排除匹配 15 例、`[9]` hunk 拆分 6 例；`mockcheck [9.5]` 增加 +行/删行 写回断言，`[20]` 增加「新文件 → 未标记审查直接更新基准 → 差异清零」回归。全部通过；打包 `change-review-0.4.2.vsix`（23 文件 77.34 KB）并装进本机 VSCode。

## 0.4.1

- **修复鼠标在块之间移动时块忽大忽小（下方所有块被顶动）**：块头的操作按钮之前用 `display:none` 隐藏、hover 才 `display:inline-flex`，按钮出现时会撑高块头、改变整块高度。改为按钮**始终占位**、只用 `visibility` 控制显隐，并给块头加 `min-height`，hover 不再引起任何高度变化。
- **统一按钮风格**（原来实心主按钮 / 次级按钮 / 红色描边 / 绿色小按钮混用，观感割裂）：现在所有按钮共用同一套描边基础（透明底 + 1px 边框 + 同圆角/行高），只用颜色区分语义，且与 diff 配色一致——接受=绿 `#3fb950`，拒绝=红 `#f85149`，已审查状态=淡绿填充，跳转/下一个/刷新=中性灰。工具栏按钮与块内小按钮现在是同一套视觉语言。
- **删除残留的「显示编辑器」按钮**：内嵌编辑器早在 0.3.9 就已移除，该按钮是遗留死代码（点了只会空转重渲染），已删除。
- 测试：`mockcheck [9.5]` 增加 4 条回归断言（无 toggleEditor/显示编辑器、块头按钮用 visibility 而非 display:none、接受=ok 绿、拒绝=danger 红）。`selfcheck` / `mockcheck` 全部通过；打包 `change-review-0.4.1.vsix`（23 文件 73.75 KB）并装进本机 VSCode。

## 0.4.0

- **「编辑」= 在对比块里就地改，不再跳到文件里改**：之前理解错了——用户要的是对比块本身可直接编辑、改完同步到文件内容，而不是「跳到文件里改」。现在对比块里属于当前文件的行（上下文行 + 新增行）都可点击直接改（`contenteditable`，带真实文件行号），失焦/回车即写回文件对应行并自动重算 diff、刷新改动列表；被删除的行（不在当前文件里）不可编辑，避免误改。
- **移除单独的「编辑」按钮**：既然对比块可直接编辑，跳转之外的编辑按钮就是冗余的，已删除；面板只剩每块一个「跳转」。
- **修复「跳转」慢 / 先停在首行再跳**：已打开的编辑器直接设选区并居中显示（不重新打开）；未打开的则带 `selection` 一次性打开到目标行，不再出现「先到首行再跳」的两段式动画。
- **忽略文件改名 `.changereviewignore` → `.crignore`**（原名太长）；默认内容精简为**一行**、中英文对照：`# 排除规则，语法同 .gitignore（每行一个 glob，# 开头为注释，空行忽略） / Exclude rules, same syntax as .gitignore ...`。语法同 .gitignore。
- **未做 `.crignore` 文件图标**：VSCode 没有「只覆盖单个文件图标」的 API，唯一方式是贡献一整套文件图标主题并要求用户切换（会替换现有主题），代价大于收益，故按用户选择不做。
- 测试：`mockcheck [9.3]` 跟随改名；`[9.5]` 改写为验证「无 textarea、无编辑按钮、对比块行可就地编辑、删除行不可编辑、editLine 写回对应行且不影响其它行」；`[20]` 断言同步适配新 markup。`selfcheck` / `mockcheck` 全部通过；打包 `change-review-0.4.0.vsix`（23 文件 72.87 KB）并装进本机 VSCode。

## 0.3.9

- **移除内嵌编辑器（隐藏的 textarea「自由编辑」）**：用户反馈不需要单独的隐藏编辑器、也不需要编辑块。改为在每个改动块的「跳转」按钮旁新增「编辑」按钮——两个按钮都直接把文件在 VSCode 主编辑器里打开到对应行（编辑即在真实编辑器中进行，保存后改动列表自动刷新）。同时清理了 `saveEdit`/`resetEdit`/`toggleEditor`/`jumpTo` 一整套内嵌编辑逻辑与样式。
- 测试：`mockcheck [9.5]` 改写为验证「面板不再内嵌 textarea、每个块有紧邻的 跳转(goto)/编辑(edit) 按钮、点编辑已在编辑器打开真实文件」；`[9]` 中旧版「不含 edit 按钮」断言同步修正；`selfcheck` / `mockcheck` 全部通过；打包 `change-review-0.3.9.vsix`（23 文件 71.26 KB）并装进本机 VSCode。

## 0.3.8

- **移除「诊断环境」按钮与功能**：诊断信息本来就会在每次刷新、探测失败、读取失败时写入 VSCode 的 **输出（Output）** 面板（命令面板搜 `Change Review` 可查看），无需再保留一个手动按钮。
- **「配置排除规则」改为直接编辑项目里的 `.changereviewignore` 文件**：不再跳进插件设置（之前设置里没有好填规则的地方，且用户明确不想写进插件设置）。点该命令会在当前工作区根创建/打开 `.changereviewignore`（gitignore 风格：每行一个 glob，`#` 注释，空行忽略），保存后自动重新探测并刷新；多工作区时先选目录。扩展会读取该文件并合并进排除规则，且始终自动隐藏 `.changereviewignore` 本身。
- **接受 / 拒绝 悬浮提示精简为核心语义**：接受 → “接受改动并标记已审查（git 下同时加入暂存区）”；拒绝 → “放弃改动，将文件还原到上次提交 (HEAD) / SVN BASE / 对比基准”。
- **审查面板 UI 整理**：内嵌编辑器默认收起（点「显示编辑器」才展开），恢复以对比块为主视图的干净布局；对比块改为卡片式（圆角边框 + 主题色增/删行背景 + 左侧色条），更贴近常规 diff 观感。
- 测试：`mockcheck [9.3]` 改为验证「不写插件设置、在工作区根创建并打开 `.changereviewignore`」；`[9]` 命令数 16→15（移除 diagnose）；`selfcheck` / `mockcheck` 全部通过；打包 `change-review-0.3.8.vsix`（22 文件 72.72 KB）并装进本机 VSCode。

## 0.3.7

- **修复标题栏图标仍是文字的问题**：`menus.view/title` 每个 entry 显式声明 `icon`（VSCode 1.85+ 在 view/title 必须显式声明，单纯在 `commands[].icon` 里写会被忽略）。refresh / nextUnreviewed / initBaseline / updateBaseline / markAllReviewed / clearReviewed / configureExclude / diagnose 全部回到图标按钮。
- **审查面板新增内嵌编辑器（"自由编辑"）**：在 diff 视图下方挂一个 `<textarea>`（带行号 gutter），初始内容 = 当前文件内容；用户直接修改 → 点「保存修改」→ 写入磁盘 → 自动重新计算 diff、刷新改动列表与状态栏（不需手动 Ctrl+S）。`refresh()` 回调复用，文件被外部改后也会同步拉新值。
- **块级「跳转」改为面板内跳**：之前跳转到原文件编辑器（你以为"无法使用"是因为没意识到要回到编辑器）；现在「跳转」在面板内直接把光标定位到内嵌编辑器里对应行（不打开新文件）；如编辑器被隐藏则回退扩展宿主 `openInEditor` 行为。
- 测试：`mockcheck [9.2]` 改为检查 view/title menu entry 里的 icon 字段；`[9.5]` 重写为「面板里含可编辑 textarea」「初始内容是当前文件」「点击 saveEdit 会写回磁盘并触发 refresh」；`selfcheck` / `vcscheck` 全部通过；打包 `change-review-0.3.7.vsix`（22 文件 72.27 KB）并装进本机 VSCode。

## 0.3.6

- **接受/拒绝按钮括号里的语义改为悬浮提示（tooltip）**：工具栏按钮文字只留「接受 / 拒绝」主动词，原先括号里写的语义（git 下为暂存 / 还原到 HEAD 或 SVN BASE 等）放进原生 `title` 属性，鼠标悬浮即可看到，不会挤按钮文字。块级按钮（接受此块 / 拒绝此块 / 跳转）同样给了悬浮提示。
- **面板工具栏去掉「在编辑器中打开并修改」按钮**：保留「跳转」（按块打开真实文件到对应行，可编辑可保存）。同时更新了 mock 测试，验证 `goto` 消息会触发 `showTextDocument` 打开真实工作区文件。
- **git / svn / 快照的判定结果直接可见，不再静默**：
  - 探测不到来源时，改动列表顶部出现 `⚠` 提示（一行说明 + 解决建议），状态栏自动切成 `$(warning)` 并提示「未识别到来源」。
  - 探测到的具体来源（Git / SVN / 快照基准）显示在状态栏 tooltip 与视图顶部信息里，可一眼判断当前走的是 git / svn / 快照哪条路径。
  - 真正失败时（如 git 不可用）只在面板顶部提示一次，不会反复弹窗刷屏。
  - `vcs/index.js` 的 `buildProviders` 新增 `failures` 字段；`detectIn` 给 svn 探测异常也加了 problems 收集。
- **多语言（i18n）支持**：`package.json` 改为 `%key%` 占位符，新增 `package.nls.json`（英文默认）与 `package.nls.zh-cn.json`（简体中文），VSCode 会按当前显示语言自动切换命令面板、设置面板、欢迎页、视图名称等所有可见文字。命令图标统一声明在 `commands[].icon` 里（不再写在 view/title），避免被 `nls` 解析时遗漏。
- **Activity Bar / 视图标题栏图标换成新版**：`resources/icon.svg` 改为「纸文件 + 文字行 + 右下角对勾」的轮廓版（`currentColor` 适配主题，绿色对勾高亮），与之前纯文字的文件图标区分更明显；视图标题栏、Activity Bar 容器都用这张图。
- **新增行配色更鲜艳**：审查面板 `.row.add` 背景从 `rgba(90,200,90,.15)` 改为 `rgba(46,160,67,.28)`，`+` 标记和右侧行号用 `#3fb950` 加粗，与 GitHub / VSCode 暗色主题一致；删除行 `.row.del` 同步调整为 `#f85149`。
- 测试：`mockcheck`（增加 [5] 接受/拒绝按钮精简 + tooltip、[9.2] 接受图标从 commands 声明、[9.2.1] nls 文件与占位符一致性、[9.2.2] icon.svg 资源存在；[9.5] 改用 `goto` 消息触发真实文件打开）；`selfcheck`（git 8 项）、`vcscheck`（svn+snapshot 70 项）全部通过；打包 `change-review-0.3.6.vsix`（22 文件，68.33 KB）并安装。

## 0.3.5

- **标题栏精简回图标按钮**：可视区只保留「刷新」（`$(refresh)`）与「下一个待审查」（`$(arrow-down)`）两个导航按钮（占 navigation@1/@2 两个固定位），避免按钮过多被 VSCode 挤成文字；全部标记/清除/诊断/排除规则等移入标题栏「…」菜单（带图标）。
  - `updateBaseline`（更新对比基准）不再对 git/svn 项目一直显示——新增 context `changeReview.snapshotActive`（存在快照基准项目且已建立基准才为真），只有这类项目才显示该按钮；`initBaseline` 仍只在无基准项目显示。
- **去掉命令标题里的 "Change Review: " 前缀**：命令面板、右键菜单、悬停提示里不再重复品牌名，直接是「进入审查模式」「接受改动」「下一个待审查」等。
- **跳转 / 在编辑器中打开并修改 → 打开真实文件**：之前这两个入口打开的是内建 diff 编辑器，看起来只读、无法直接改。现在改为直接打开工作区里的真实文件并定位到改动行，可自由编辑，保存后改动列表与审查面板自动刷新（文件级「在编辑器中打开差异」仍在右键菜单保留）。
- **排除规则入口（原来找不到在哪填）**：新增命令「配置排除规则…」，点击即打开 VSCode 设置并定位 `changeReview.exclude`；入口放在改动列表标题栏「…」菜单、文件/仓库右键菜单、欢迎页里；`exclude` 配置的说明文字同步补了填写位置指引。
- **性能优化（实测基准见下）**：
  - 接受/标记不再触发整表重扫：git 接受只是进暂存区、工作区内容没变，改为 0 次 git 调用直接更新内存状态 + 树刷新；
  - 拒绝（整文件/单个块）后只复查那一个文件（git 单文件 status，干净 1 次调用、仍改动 2 次调用），不再整仓 status+numstat；
  - 探测结果缓存：配置与工作区目录没变时，自动/定时刷新不再重复 `git rev-parse` 等探测；
  - 无改动仓库的整表刷新从 3 次 git 调用降到 1 次（去掉多余的 rev-parse HEAD 与 numstat）；
  - diff 结果缓存：同一文件内容没变时再次渲染面板不再重新 spawn git；
  - 全量刷新后若当前面板文件实际没变，跳过面板重渲染（省一次 diff）；
  - 保存文件（Ctrl+S）只复查被保存的那一个文件，不再整表重扫。
  - 本机实测（Windows，单次 git 进程约 230ms；300 文件仓库）：接受改动由 ~1.5s（多次 spawn + 整仓扫描）降到 <50ms；拒绝还原整文件由整仓刷新降到单文件复查（干净 235ms / 仍改动 450ms）；无改动自动刷新 1 次调用约 250ms；重复切换文件渲染 diff 命中缓存 ≈0ms。
- 测试：mockcheck 新增 [9.2]（标题无前缀、标题栏图标与 when 声明）、[9.3]（排除规则入口命令）、[9.4]（接受走轻量路径）、[9.5]（跳转/编辑打开真实文件）；mock/self/vcs 三套全绿。

## 0.3.4

- **标题栏新增图标按钮**：`initBaseline`（`$(add)`）、`updateBaseline`（`$(arrow-up)`）、`markAllReviewed`（`$(check-all)`）、`clearReviewed`（`$(clear-all)`）、`diagnose`（`$(info)`）都改为图标按钮，不再折叠到「更多操作」菜单。`initBaseline` 仅在无基准时显示，`updateBaseline` 仅在有基准时显示，二者用 context `changeReview.hasBaseline` 互斥。
- **添加键盘快捷键**（仅在 Change Review 视图聚焦时生效，不与 VSCode 内置快捷键冲突）：
  - `Ctrl+Enter` — 进入审查面板
  - `Ctrl+Shift+A` — 接受
  - `Ctrl+Shift+R` — 拒绝
  - `Ctrl+Shift+M` — 标记 / 取消已审查（同一个键位，根据状态自动切换）
  - `Alt+N` — 下一个待审查文件
  - macOS 对应 `Cmd` 替换 `Ctrl`
- 测试：mockcheck 新增 [9.1] 快捷键声明段，验证快捷键齐全且都限定在 `focusedView == changeReview.files` 范围内。95 项全绿。

## 0.3.3

- **手动排除规则改名为通用 `changeReview.exclude`**（取代 `changeReview.svnIgnore`）：
  - 默认空数组，由用户自填 glob 模式，作用方式类似 vscode 的 `search.exclude`；
  - 对 git/svn/快照所有来源的所有改动都生效（覆盖 M/A/D/?/~ 等全部 kind），不再只针对 svn 未版本化项；
  - 想要保留默认建议就在扩展设置里直接编辑数组（数组是整体替换语义）；
  - `changeReview.snapshotExclude` 保留，它仍然是「建基准时的排除」，与运行时列表的 `exclude` 语义不同。
- **标题栏两个命令改图标按钮**：刷新（`$(refresh)`）、下一个待审查文件（`$(arrow-down)`），鼠标悬停显示 tooltip（命令 title），纯文字的「Change Review: …」前缀去掉。
- 测试：vcscheck 把旧「SVN 忽略」段改为「通用 exclude」段，断言默认空、对所有 kind 生效、自定义规则生效；mockcheck 87 项、selfcheck 全绿。

## 0.3.2

- **SVN 未版本化项支持忽略规则（`changeReview.svnIgnore`）**：svn 没有项目内统一的 .gitignore，仓库又没设 `svn:ignore` 时，`build/`、`dist/` 等生成物会以 `?` 出现并和真正的新文件混在一起。
  - 默认忽略常见生成物（node_modules / dist / build / out / target / bin / obj / *.log 等），只影响 svn 的未版本化项，版本化的修改/删除不受影响；
  - svn 原生认得的 `svn:ignore` / `global-ignores` 仍然由 `svn status` 直接过滤，本配置是本地补充（两者同时生效）；
  - 数组是整体替换语义（与 VSCode 数组设置一致），设 `[]` 关闭额外忽略。
- **向上探测上限锁死为 5 层**：`changeReview.vcsSearchDepth` 的最大值从 20 改为 5——超过 5 层的 `.svn` / `.git` 一律不接管（按无版本控制处理），避免误认到无关的上级仓库。默认仍是 5。
- 内置快照排除列表与 SVN 忽略列表统一为一份（`util.DEFAULT_EXCLUDE`）。
- 测试：vcscheck 新增「.svn 超过 5 层不接管」「SVN 忽略（内置/关闭/自定义/版本化项不误伤）」真实仓库场景。

## 0.3.1

- **修复：打开仓库/工作副本的子目录时，列出的是整个仓库的改动。**
  - 场景：svn 工作副本根 `A/.svn` 下并行 `prj1`、`prj2`、`prj1-backup`（未版本化），VSCode 只打开 `A/prj1`，此前会把 `prj1`、`prj2`、`prj1-backup` 的改动全部列出。
  - 现在探测到仓库根高于打开目录时，会记下「打开目录范围」，改动列表只保留该目录内的文件（git / svn 都生效）；git 还额外把目录传给 `git status` 避免全库扫描。
  - 直接打开仓库根目录时行为不变（仍显示全部）。
  - 多文件夹工作区命中同一仓库根时，各打开目录的范围取并集。
- 诊断命令输出增加「打开目录范围」一行，便于确认。
- 测试新增：vcscheck 覆盖 svn/git 子目录范围场景（真实仓库），mockcheck 覆盖扩展主流程在子目录工作区下的树/徽章/审查。

## 0.3.0

- **大版本：支持 SVN 与无版本控制项目（快照基准）**，来源自动探测（git → svn → 快照），也可用 `changeReview.forceVcs` 强制。
- **SVN 支持**：`.svn` 可能在打开的文件夹的上级甚至上上级，按 `changeReview.vcsSearchDepth`（默认 5 层）逐级向上探测；改动列表来自 `svn status` + `svn cat -r BASE`；
  - 接受 = 保留改动并标记已审查（SVN 无暂存区）；
  - 拒绝 = `svn revert` 还原到 BASE，未版本化文件删除；
  - 块级操作基于纯 JS 行级 diff，不受 svn 输出换行风格影响。
- **快照基准（无 git / svn 项目）**：执行「初始化对比基准」把当前文件状态记录到扩展的 globalStorage（不污染项目目录），之后以此为基准审查本地改动；可「更新对比基准」；
  - 默认排除 `node_modules / dist / build / .git / .svn / *.log` 等（`changeReview.snapshotExclude`），可调大小/数量上限；
  - 拒绝 = 把文件写回基准内容、新增文件删除。
- **新命令**：「Change Review: 初始化对比基准」「Change Review: 更新对比基准」（可只更新选中的文件），无 git/svn 项目时视图有欢迎页引导。
- **新配置**：`svnPath`、`forceVcs`、`vcsSearchDepth`、`snapshotExclude`、`snapshotMaxFileSize`、`snapshotMaxFiles`。
- **块级拒绝改为纯 JS 反向替换**（不再依赖 `git apply -R`），git / svn / 快照三种来源行为一致：只还原目标块、保留上下文与换行风格、内容对不上时明确报错。
- 内置纯 JS 行级 diff 引擎（前后缀裁剪 + Myers + 公共锚点分段）：2 万行文件的局部改动也能秒出 diff，差异过大自动退化为整段替换保证不卡死。
- 诊断命令扩充：git/svn 候选、识别到的每个来源（类型/基准/文件数/快照是否已建）、快照基准存放位置。

## 0.2.0

- **修复：审查面板按钮全部无效**。根因是 `createWebviewPanel` 漏传 `enableScripts: true`（VSCode 默认禁用 webview 脚本），内联脚本从未执行，按钮点击发不出任何消息。现在面板脚本加载后会向 Output 面板报告 ready。
- **新增：按块接受/拒绝**
  - 每个改动块（hunk）悬停出现「接受此块 / 拒绝此块 / 跳转」；
  - 接受此块 = `git apply --cached` 只暂存这一块，工作区不动；拒绝此块 = `git apply -R` 只还原这一块；
  - 块级"已接受"状态持久化（按内容签名，文件再改动自动失效），已接受的块变暗并带 ✓；
  - 一个文件的全部块都接受后自动给文件打钩；
  - 未跟踪/删除/冲突文件不提供块级接受（暂存上下文对不上），只保留块级还原或文件级操作。
- 审查面板的每条消息、每次 git 动作都写入 Output 面板（`Change Review` 通道），方便排查。
- 点击的块如果和当前 diff 对不上（文件被编辑过），提示刷新而不是错误地操作别的块。

## 0.1.1

- 新增平台适配层：Windows / Linux(WSL) / macOS 分别有各自的 git 候选路径，自动探测并缓存
- 自动识别「Linux 下跑着 Windows 版 git」（反之亦然），发现路径体系不匹配就换用下一个候选
- 声明 `extensionKind: ["workspace"]`，确保在 WSL / 远程里以 workspace 扩展运行
- 检测到远程(WSL)工作区却运行在本地宿主时给出明确警告
- git 报 `dubious ownership` 时弹提示，可一键加 `safe.directory`
- 命令取不到目标文件时不再静默跳过：依次回退到「树选中项 → 当前编辑器打开的文件 → 审查面板当前文件」，都没有就弹提示
- 所有命令统一 try/catch，失败时同时弹错误通知 + 写入 Output 面板
- 右键菜单对已审查文件也显示「进入审查 / 打开差异 / 接受 / 拒绝」
- 新增命令「Change Review: 诊断环境」与设置 `changeReview.gitPath`
- 修复：命令包装器吞掉返回值；「全部标记已审查」未同步内存状态导致徽章不更新

## 0.1.0

- 列出与上次 commit 不同的本地文件，显示 `+新增 / −删除` 行数
- 每个文件可打钩标记已审查，状态持久化；文件被再次改动时自动取消打钩
- Activity Bar 徽章 + 状态栏进度
- 点击文件名进入审查模式：统一视图 diff、按块还原、接受 / 拒绝改动
- 在原生 diff 编辑器中打开，右侧可直接编辑
- 「下一个待审查」循环跳转，多仓库工作区分组
