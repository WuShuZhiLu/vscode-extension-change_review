## 0.5.0-pre1
- 核心规则落地：接受 / 拒绝（文件级与块级）只记录决定，不改文件、不进暂存区
- 所有改动块都有决定后自动标记已审查；git add / 还原 / 删除文件在标记那一刻统一执行
- git 工程：任何「标记为已审查」都加入暂存区（git add），取消勾选 = git reset
- 新增「取消接受」，与「撤销拒绝」对称
- 标记已审查后自动跳到下一个待审查文件
- 手动「取消审查」后文件未再改时不会被自动标记打回；文件改动后恢复自动标记
- 多仓库工作区：打开目录不是仓库时自动扫一层子目录找 git/svn 仓库
- 「拒绝全部」不再弹确认框
- 树右键菜单分工：文件节点「忽略该文件」（写 `.crignore`），仓库节点「配置排除规则…」；「清除所有审查标记」更名「全部清除已审查」
- 修复面板右键菜单在 diff 正文弹不出来、审查中标记跨刷新丢失
- 面板按钮文案：文件已审查后直接显示「取消审查」
- 排除规则回退 `.gitignore`；「屏蔽此文件」只保留在面板右键菜单
- 迭代提速：`mockcheck --only <场景名>` 按场景跑测试；release 直接用预装 vsce 打包
- 新增 CI/CD（GitHub + Gitea）：打 tag 即自动发版
- 文档精简（README / CHANGELOG 只保留介绍、功能、变化）

## 0.4.20
- 面板按钮文案：文件已审查后，按钮直接显示「取消审查」（不再用「已审查 ✓（点击取消）」的括号提示）；未审查时显示「标记已审查」
- 修「块都打钩了，却没标记已审查」：
- 测试：mockcheck 新增 [23]，覆盖 4 种真实顺序
## 0.4.19
- 修复「屏蔽此文件」写入 `.crignore` 后不生效，并把排除匹配对齐 `.gitignore` 语义
- 测试：selfcheck 新增 [8.1]（`matchExcludeSets` / `relFromBase` 锚定语义，含"不误伤规则文件目录之外的同名路径"）；mockcheck [22]（打开仓库子目录时…
## 0.4.17
- 新增「屏蔽此文件」：审查面板里可一键把当前文件写入所在工作区目录的 `.crignore`（gitignore 风格相对路径规则），写完后立即重新探测刷新，文件从改动列表消失
- mockcheck 新增 [9.3.1]：右键屏蔽写入 `.crignore`、文件从列表消失、重复屏蔽不重复追加；面板 HTML 含右键菜单容器与按钮
## 0.4.15
- 排除规则回退到 .gitignore：没有 `.crignore` 时，默认采用该目录 `.gitignore` 的规则（git 工程不用再额外配 `.crignore`）；有 `.crignore` 时仍以它为准
- 整行选中 + Backspace 不再错误合并：合并行只在「无选区且光标贴边」时触发；有选区时按 Backspace/Delete 走正常删除内容
- 撤销/重做覆盖行间操作：新增文件级快照栈（扩展侧，跨面板重渲染不丢）
- 修复"撤销→重做→再撤销失效"：根因是面板重渲染会把 webview 里的撤销栈清空，现在栈放在扩展侧，不再受重渲染影响
- mockcheck 新增：gitignore 回退、合并行需无选区、文件级撤销/重做（插行可撤销、可重做、再撤销仍有效）
## 0.4.14
- Ctrl+V / Ctrl+C / Ctrl+X 真正生效：不再依赖浏览器默认行为和 `execCommand`（webview 里常被 VSCode 拦下），改为面板主动调 `navigator.clipboa…
- 行中回车不再卡：行级编辑（写回、插行、删行、合并）的收尾从"整仓全量刷新 doRefresh"改成"只复查这一个文件 refreshSingleEntry"，去掉整仓 git 扫描，行内编辑响应明显变快
- 合并行与删行：Backspace 在行首 → 并进上一行；Delete 在行尾 → 把下一行并上来；Ctrl+Shift+K（编辑器同款）可整行删除内容行，空行 Backspace/Delete、Ctrl+Del…
- mockcheck 新增断言：上行/下行合并（文件内容与行数实测）、剪贴板自行接管、行内编辑不再触发全量刷新
## 0.4.13
- Tab 跟随 VSCode 设置：面板内联编辑的 Tab 插入内容按 `editor.insertSpaces` / `editor.tabSize` 决定（空格还是制表符、几个空格），不再固定插 `\t`
- Enter 插行方向按光标位置：光标在行首 → 在本行上方插空行（焦点留在原内容行，内容顶下去）；光标在行尾 → 下方插空行；光标在行中 → 像编辑器一样拆行（前半留本行、后半成下一行）
- Ctrl 组合键在行编辑中生效：Ctrl+S 保存当前行（内容已最新时给明确提示，不再静默）；Ctrl+Z / Ctrl+Shift+Z(Ctrl+Y) 撤销重做（自维护撤销栈，跨面板重渲染仍有效）；Ctrl+C…
- mockcheck 新增 [18.1]：上行插行、拆行、Tab 缩进注入、快捷键接管均有实测断言
## 0.4.12
- 纠正删除对象：移除的是树视图标题栏的「下一个待审查」图标按钮，panel 里的文字按钮保留
## 0.4.11
- 面板移除「下一个待审查」按钮（反复出问题，弃用）；树视图命令与审查完成后的自动跳转保留
- 修复 Linux"0 改动"假象：打开目录与仓库根路径不一致（符号链接/挂载点）时，`../..` 被当作 git pathspec 传入导致匹配不到任何文件
- 扩展图标白色背景改真透明：圆角方块外的四角像素 alpha 置 0
- 面板语言说明：`changeReview.uiLanguage` 设为 `zh` 可强制中文（auto 跟随 VSCode 显示语言；VSCode 本体是英文界面时 auto 会判成英文）
## 0.4.10
- 修复"部分拒绝部分接受"后已审查标记丢失：拒绝块执行还原后文件内容变化，旧的按内容指纹校验立刻失配，文件显示回"未审查"
- Linux 诊断强化：日志同时落盘到 `globalStorage/change-review.log`（终端可直接 cat/tail，Output 面板找不到时日志也不丢）；git 层所有命令失败、status…
- 日志查看：命令面板「Change Review: 打开日志」，或终端 `cat ~/.../globalStorage/local/change-review.log`
## 0.4.9
- 全部块都有决定（接受或拒绝）→ 自动标记已审查：拒绝块在此刻统一执行还原，随后立即复查该文件
- 文件审查完成后自动跳下一个：拒绝执行还原后文件无差异 → 从列表移除并自动跳到下一个待审查；手动「标记已审查」同理
- 修复拒绝块无任何视觉变化：根因是 `rejectedSigs` 没传进块渲染函数，拒绝状态从未被渲染
## 0.4.8
- 与 0.4.7 二轮内容相同（拒绝块不再立即删除、uiLanguage 语言设置、日志可见性、next 去锁），独立升号以便区分安装包
## 0.4.7
- 修复拒绝块仍立即还原内容：根因是「全部块都有决定就自动标记已审查」把拒绝决定也当成了完成
- 面板语言可强制指定：新设置 `changeReview.uiLanguage`（auto / zh / en），auto 跟随 VSCode 显示语言
- 日志终于能看到了：新增命令「Change Review: 打开日志」；所有错误弹窗自动打开输出面板；日志同步写 console（Developer Tools 可见）；扩展激活即写一条日志（含版本与语言检测）
- 「下一个待审查」去掉防重入锁（锁曾在异常路径卡死导致只有第一次生效），每一步写日志、出错弹窗并自动打开日志面板
- 拒绝块延迟执行（与接受块对称）、拒绝全部后自动标记已审查（0.4.7 首轮引入，继续生效）
- 拒绝块改为延迟执行（与接受块对称）：点「拒绝此块」只记录拒绝决定，不再立即改文件；块上显示红色「拒绝·待执行」徽章与红色左条，按钮变「撤销拒绝」
- 拒绝全部后自动标记已审查：与接受全部对称，还原完成后文件自动打钩并从改动列表消失
- 文件整体标记已审查后，块级「接受/拒绝」按钮隐藏（0.4.6 已引入，本版继续生效），跳转保留
## 0.4.6（本地打包实测用 · 未发布到市场；版本号冻结，后续修改不再升号，直到用户同意）
- 面板文字 i18n（中英双语）：面板内所有文字（按钮、提示、tooltip、状态标签）随 VSCode 显示语言自动切换
- 插件图标（AI 生成）：用 ImageGen 生成正式图标（256x256 PNG，深底白文件+绿对勾），替换掉之前手绘的丑图标
- Linux git 诊断：`git status` 返回数据但解析出 0 条改动时，把原始输出写到 stderr 帮助定位 Linux 解析问题；`parseDubiousRoot` 兼容 Linux 的引号路径
- Activity Bar 图标重绘：内容撑满画布（边距从 22px 缩到 4px），减少空白；插件包新增 `resources/icon.png`（扩展列表/市场图标，package.json `icon` 字段）
- 「下一个待审查」加防重入锁 + 错误上报：连续快速点击不再互相干扰；如果内部出错，用户会看到错误提示而不是静默无反应
- 接受全部后隐藏块级按钮：文件整体已接受（已审查）后，块级「接受此块 / 拒绝此块」按钮自动隐藏，避免矛盾操作
- 树高亮"审查中"标记（不依赖 reveal）：当前正在审查的文件在改动列表的 description 里显示 `◀ 审查中` 后缀
- 交互方向纠正（用户明确要求）：「跳转」不再打开单文件编辑器，改为打开 VSCode 原生左右对比视图（左=基准只读，右=工作区文件），并且光标直接定位到改动行
- 修复「拒绝全部」对新增文件不删除（真根因）：新增文件只要进过暂存区（标记已审查 = git add），git 状态从 `??` 变成 `A`（kind=added），旧代码走"还原到 HEAD"分支
- 修复复制不可靠：复制改由扩展宿主写系统剪贴板（webview 内 execCommand('copy') 不稳定）；另外补了兜底：没点进某行但有选中行时，Ctrl+V 直接插到最后选中的文件行下方
- 树高亮跟随再加两道保险：把"当前该高亮的文件"记下来；改动列表从隐藏变可见时、以及每次刷新完成后，都会自动再补一次 reveal
- 修复面板"什么功能都失效"的真正兜底：用 jsdom 实测确认脚本在正确环境下能跑（点行号→选中、双击→整段、复制→`copyText` 全部正常），但用户实测看不到效果
- 修复 SVN「拒绝」新增文件留空文件的 bug：原来 `svn revert` 在"文件已被 `svn add` 但未提交"或"BASE 是空文件"两种场景下会把工作区文件写空，但用户期望"全清零"应直接删
- 修复面板所有按钮/快捷键失效（拒绝、接受、标记已审查、Tab、Ctrl+S、多选、换行全部没反应，0.4.6 首轮实测发现）
- 行编辑 Tab = 制表符（不再跳焦点）；Ctrl+S = 立即保存该行并刷新 diff，保存后光标仍留在该行
- 树高亮跟随再次加固：点「下一个待审查」改为"先试高亮 → 切面板 → 再补等一轮"两段式；面板里「标记已审查/接受」后也会 reveal，打钩文件下移时高亮跟着走
- 多行选择 + 复制/删除/粘贴（按用户确认的方案）：点行号选中、Ctrl/Cmd+点多选、Shift 连选、双击行号选中整段、Ctrl+A 全选；浮动工具条「复制 / 删除选中 / 还原此段 / ✕」，Ctrl+…
- 块内"段"级还原：一块里的每一段连续改动（如"上面一处修改 + 两行后一处纯删除"）可单独「还原此段」
## 0.4.5
- 接受/拒绝不再碰 git 暂存区（语义重构）：接受（全部/此块）只把改动标记为已接受并给文件打钩
- 移除行级「+行 / 删行」按钮（用户要求不要了）：增删行只走键盘
- 编辑行时 ↑/↓ 在可编辑行之间切换：失焦自动写回当前行，光标落到上一/下一行
- 修复「下一个待审查」后树高亮与面板不一致：找不到节点时中途强制刷新树再重试 reveal（`expand` 展开父节点），避免列表高亮停在旧文件
## 0.4.4
- 修复删行后焦点跳错行（如：删 19 行焦点跑到 21 行、20 行原地不动；20 行回车出的空行删掉后不回 20）
## 0.4.3
- 修复「下一个待审查」后面板切了、改动列表高亮不跟随：树数据源现在会记录每个文件的当前节点实例，切下一个文件后用 `treeView.reveal` 把对应节点滚动到可见并选中，面板与改动列表始终指同一个文件
- 修复无管理项目「更新基准」只更新了当前选中的那一个文件（0.4.2 bug 10 截图现象：package.json 清了、其余文件差异原样）：`updateBaseline` 之前走 `resolveArg` …
- 相近的多处改动恢复合并在同一块（撤销 0.4.2 的按簇拆分）：行内相近修改就是同一处编辑意图，保持与 `git -U3` 输出一致，一个块整体接受/拒绝；`splitHunk` 相关代码全部移除，`selfch…
- 对比块里支持键盘删行：空行上按 Backspace / Delete 删除该行（与 Enter 插入行对称）；任意行按 Ctrl/Cmd+Delete 直接删整行
## 0.4.2
- 修复 `.crignore` 文件夹排除无效：glob 匹配只认 `/dir/` 形式，`build/`、`.vscode/`、`test/` 这类 gitignore 目录写法生成的正则是 `^build/$`…
- 统一 diff 增删行颜色：新增行背景不再用主题变量 `diffEditor-insertedLineBackground`（那是另一种绿，和按钮的 `#3fb950` 对不上），改为直接由按钮/徽章同款色相派生…
- 对比块编辑支持增删行：每行悬浮出现「+行 / 删行」按钮（绝对定位 + opacity，不抖动布局）；编辑时按 Enter 直接在该行下面插入一行并自动聚焦新行（Esc 仍是放弃修改）
- 修复两处相近改动被并进一个块：`git -U3` / 内置 diff 引擎会把间隔不超过上下文行数的几处改动合并成一个 hunk，导致「接受/拒绝此块」连带无关改动
- 修复无管理项目「更新基准」后差异仍在：全量更新基准只遍历已有基准索引，untracked 新文件永远进不了基准
- 测试：`selfcheck` 新增 `[8]` 排除匹配 15 例、`[9]` hunk 拆分 6 例；`mockcheck [9.5]` 增加 +行/删行 写回断言，`[20]` 增加「新文件 → 未标记审查直…
## 0.4.1
- 修复鼠标在块之间移动时块忽大忽小（下方所有块被顶动）：块头的操作按钮之前用 `display:none` 隐藏、hover 才 `display:inline-flex`，按钮出现时会撑高块头、改变整块高度
- 统一按钮风格（原来实心主按钮 / 次级按钮 / 红色描边 / 绿色小按钮混用，观感割裂）：现在所有按钮共用同一套描边基础（透明底 + 1px 边框 + 同圆角/行高），只用颜色区分语义，且与 diff 配色一致
- 删除残留的「显示编辑器」按钮：内嵌编辑器早在 0.3.9 就已移除，该按钮是遗留死代码（点了只会空转重渲染），已删除
- 测试：`mockcheck [9.5]` 增加 4 条回归断言（无 toggleEditor/显示编辑器、块头按钮用 visibility 而非 display:none、接受=ok 绿、拒绝=danger 红）
## 0.4.0
- 「编辑」= 在对比块里就地改，不再跳到文件里改：之前理解错了
- 移除单独的「编辑」按钮：既然对比块可直接编辑，跳转之外的编辑按钮就是冗余的，已删除；面板只剩每块一个「跳转」
- 修复「跳转」慢 / 先停在首行再跳：已打开的编辑器直接设选区并居中显示（不重新打开）；未打开的则带 `selection` 一次性打开到目标行，不再出现「先到首行再跳」的两段式动画
- 忽略文件改名 `.changereviewignore` → `.crignore`（原名太长）；默认内容精简为一行、中英文对照：`# 排除规则，语法同 .gitignore（每行一个 glob，# 开头为注释，…
- 未做 `.crignore` 文件图标：VSCode 没有「只覆盖单个文件图标」的 API，唯一方式是贡献一整套文件图标主题并要求用户切换（会替换现有主题），代价大于收益，故按用户选择不做
- 测试：`mockcheck [9.3]` 跟随改名；`[9.5]` 改写为验证「无 textarea、无编辑按钮、对比块行可就地编辑、删除行不可编辑、editLine 写回对应行且不影响其它行」；`[20]` 断…
## 0.3.9
- 移除内嵌编辑器（隐藏的 textarea「自由编辑」）：用户反馈不需要单独的隐藏编辑器、也不需要编辑块
- 测试：`mockcheck [9.5]` 改写为验证「面板不再内嵌 textarea、每个块有紧邻的 跳转(goto)/编辑(edit) 按钮、点编辑已在编辑器打开真实文件」；`[9]` 中旧版「不含 edit …
## 0.3.8
- 移除「诊断环境」按钮与功能：诊断信息本来就会在每次刷新、探测失败、读取失败时写入 VSCode 的 输出（Output） 面板（命令面板搜 `Change Review` 可查看），无需再保留一个手动按钮
- 「配置排除规则」改为直接编辑项目里的 `.changereviewignore` 文件：不再跳进插件设置（之前设置里没有好填规则的地方，且用户明确不想写进插件设置）
- 接受 / 拒绝 悬浮提示精简为核心语义：接受 → “接受改动并标记已审查（git 下同时加入暂存区）”；拒绝 → “放弃改动，将文件还原到上次提交 (HEAD) / SVN BASE / 对比基准”
- 审查面板 UI 整理：内嵌编辑器默认收起（点「显示编辑器」才展开），恢复以对比块为主视图的干净布局；对比块改为卡片式（圆角边框 + 主题色增/删行背景 + 左侧色条），更贴近常规 diff 观感
- 测试：`mockcheck [9.3]` 改为验证「不写插件设置、在工作区根创建并打开 `.changereviewignore`」；`[9]` 命令数 16→15（移除 diagnose）；`selfcheck…
## 0.3.7
- 修复标题栏图标仍是文字的问题：`menus.view/title` 每个 entry 显式声明 `icon`（VSCode 1.85+ 在 view/title 必须显式声明，单纯在 `commands[].ic…
- 审查面板新增内嵌编辑器（"自由编辑"）：在 diff 视图下方挂一个 `<textarea>`（带行号 gutter），初始内容 = 当前文件内容；用户直接修改 → 点「保存修改」→ 写入磁盘 → 自动重新计算 …
- 块级「跳转」改为面板内跳：之前跳转到原文件编辑器（你以为"无法使用"是因为没意识到要回到编辑器）；现在「跳转」在面板内直接把光标定位到内嵌编辑器里对应行（不打开新文件）；如编辑器被隐藏则回退扩展宿主 `openI…
- 测试：`mockcheck [9.2]` 改为检查 view/title menu entry 里的 icon 字段；`[9.5]` 重写为「面板里含可编辑 textarea」「初始内容是当前文件」「点击 sav…
## 0.3.6
- 接受/拒绝按钮括号里的语义改为悬浮提示（tooltip）：工具栏按钮文字只留「接受 / 拒绝」主动词，原先括号里写的语义（git 下为暂存 / 还原到 HEAD 或 SVN BASE 等）放进原生 `title`…
- 面板工具栏去掉「在编辑器中打开并修改」按钮：保留「跳转」（按块打开真实文件到对应行，可编辑可保存）
- git / svn / 快照的判定结果直接可见，不再静默：
- 多语言（i18n）支持：`package.json` 改为 `%key%` 占位符，新增 `package.nls.json`（英文默认）与 `package.nls.zh-cn.json`（简体中文），VSCo…
- Activity Bar / 视图标题栏图标换成新版：`resources/icon.svg` 改为「纸文件 + 文字行 + 右下角对勾」的轮廓版（`currentColor` 适配主题，绿色对勾高亮），与之前纯…
- 新增行配色更鲜艳：审查面板 `.row.add` 背景从 `rgba(90,200,90,.15)` 改为 `rgba(46,160,67,.28)`，`+` 标记和右侧行号用 `#3fb950` 加粗，与 Gi…
- 测试：`mockcheck`（增加 [5] 接受/拒绝按钮精简 + tooltip、[9.2] 接受图标从 commands 声明、[9.2.1] nls 文件与占位符一致性、[9.2.2] icon.svg 资…
## 0.3.5
- 标题栏精简回图标按钮：可视区只保留「刷新」（`$(refresh)`）与「下一个待审查」（`$(arrow-down)`）两个导航按钮（占 navigation@1/@2 两个固定位），避免按钮过多被 VSCod…
- 去掉命令标题里的 "Change Review: " 前缀：命令面板、右键菜单、悬停提示里不再重复品牌名，直接是「进入审查模式」「接受改动」「下一个待审查」等
- 跳转 / 在编辑器中打开并修改 → 打开真实文件：之前这两个入口打开的是内建 diff 编辑器，看起来只读、无法直接改
- 排除规则入口（原来找不到在哪填）：新增命令「配置排除规则…」，点击即打开 VSCode 设置并定位 `changeReview.exclude`；入口放在改动列表标题栏「…」菜单、文件/仓库右键菜单、欢迎页里；`…
- 性能优化（实测基准见下）：
- 测试：mockcheck 新增 [9.2]（标题无前缀、标题栏图标与 when 声明）、[9.3]（排除规则入口命令）、[9.4]（接受走轻量路径）、[9.5]（跳转/编辑打开真实文件）；mock/self/vc…
## 0.3.4
- 标题栏新增图标按钮：`initBaseline`（`$(add)`）、`updateBaseline`（`$(arrow-up)`）、`markAllReviewed`（`$(check-all)`）、`clea…
- 添加键盘快捷键（仅在 Change Review 视图聚焦时生效，不与 VSCode 内置快捷键冲突）：
- 测试：mockcheck 新增 [9.1] 快捷键声明段，验证快捷键齐全且都限定在 `focusedView == changeReview.files` 范围内
## 0.3.3
- 手动排除规则改名为通用 `changeReview.exclude`（取代 `changeReview.svnIgnore`）：
- 标题栏两个命令改图标按钮：刷新（`$(refresh)`）、下一个待审查文件（`$(arrow-down)`），鼠标悬停显示 tooltip（命令 title），纯文字的「Change Review: …」前缀去掉
- 测试：vcscheck 把旧「SVN 忽略」段改为「通用 exclude」段，断言默认空、对所有 kind 生效、自定义规则生效；mockcheck 87 项、selfcheck 全绿
## 0.3.2
- SVN 未版本化项支持忽略规则（`changeReview.svnIgnore`）：svn 没有项目内统一的 .gitignore，仓库又没设 `svn:ignore` 时，`build/`、`dist/` 等生…
- 向上探测上限锁死为 5 层：`changeReview.vcsSearchDepth` 的最大值从 20 改为 5
- 内置快照排除列表与 SVN 忽略列表统一为一份（`util.DEFAULT_EXCLUDE`）
- 测试：vcscheck 新增「.svn 超过 5 层不接管」「SVN 忽略（内置/关闭/自定义/版本化项不误伤）」真实仓库场景
## 0.3.1
- 修复：打开仓库/工作副本的子目录时，列出的是整个仓库的改动
- 诊断命令输出增加「打开目录范围」一行，便于确认
- 测试新增：vcscheck 覆盖 svn/git 子目录范围场景（真实仓库），mockcheck 覆盖扩展主流程在子目录工作区下的树/徽章/审查
## 0.3.0
- 大版本：支持 SVN 与无版本控制项目（快照基准），来源自动探测（git → svn → 快照），也可用 `changeReview.forceVcs` 强制
- SVN 支持：`.svn` 可能在打开的文件夹的上级甚至上上级，按 `changeReview.vcsSearchDepth`（默认 5 层）逐级向上探测；改动列表来自 `svn status` + `svn c…
- 快照基准（无 git / svn 项目）：执行「初始化对比基准」把当前文件状态记录到扩展的 globalStorage（不污染项目目录），之后以此为基准审查本地改动；可「更新对比基准」；
- 新命令：「Change Review: 初始化对比基准」「Change Review: 更新对比基准」（可只更新选中的文件），无 git/svn 项目时视图有欢迎页引导
- 新配置：`svnPath`、`forceVcs`、`vcsSearchDepth`、`snapshotExclude`、`snapshotMaxFileSize`、`snapshotMaxFiles`
- 块级拒绝改为纯 JS 反向替换（不再依赖 `git apply -R`），git / svn / 快照三种来源行为一致：只还原目标块、保留上下文与换行风格、内容对不上时明确报错
- 内置纯 JS 行级 diff 引擎（前后缀裁剪 + Myers + 公共锚点分段）：2 万行文件的局部改动也能秒出 diff，差异过大自动退化为整段替换保证不卡死
- 诊断命令扩充：git/svn 候选、识别到的每个来源（类型/基准/文件数/快照是否已建）、快照基准存放位置
## 0.2.0
- 修复：审查面板按钮全部无效
- 新增：按块接受/拒绝
- 审查面板的每条消息、每次 git 动作都写入 Output 面板（`Change Review` 通道），方便排查
- 点击的块如果和当前 diff 对不上（文件被编辑过），提示刷新而不是错误地操作别的块
## 0.1.1
- 新增平台适配层：Windows / Linux(WSL) / macOS 分别有各自的 git 候选路径，自动探测并缓存
- 自动识别「Linux 下跑着 Windows 版 git」（反之亦然），发现路径体系不匹配就换用下一个候选
- 声明 `extensionKind: ["workspace"]`，确保在 WSL / 远程里以 workspace 扩展运行
- 检测到远程(WSL)工作区却运行在本地宿主时给出明确警告
- git 报 `dubious ownership` 时弹提示，可一键加 `safe.directory`
- 命令取不到目标文件时不再静默跳过：依次回退到「树选中项 → 当前编辑器打开的文件 → 审查面板当前文件」，都没有就弹提示
- 所有命令统一 try/catch，失败时同时弹错误通知 + 写入 Output 面板
- 右键菜单对已审查文件也显示「进入审查 / 打开差异 / 接受 / 拒绝」
- 新增命令「Change Review: 诊断环境」与设置 `changeReview.gitPath`
- 修复：命令包装器吞掉返回值；「全部标记已审查」未同步内存状态导致徽章不更新
## 0.1.0
- 列出与上次 commit 不同的本地文件，显示 `+新增 / −删除` 行数
- 每个文件可打钩标记已审查，状态持久化；文件被再次改动时自动取消打钩
- Activity Bar 徽章 + 状态栏进度
- 点击文件名进入审查模式：统一视图 diff、按块还原、接受 / 拒绝改动
- 在原生 diff 编辑器中打开，右侧可直接编辑
- 「下一个待审查」循环跳转，多仓库工作区分组
